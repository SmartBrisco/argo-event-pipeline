apiVersion: v1
kind: Service
metadata:
  name: webhook-eventsource-svc
  namespace: argo-events
spec:
  selector:
    app: webhook
  ports:
    - protocol: TCP
      port: 12000
      targetPort: 12000
